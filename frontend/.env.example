# API base URL (no trailing slash). Used in development and build.
# For local dev, backend is usually at http://127.0.0.1:8000/api
# For production (e.g. Vercel), set to your deployed backend URL.
VITE_API_BASE_URL=http://127.0.0.1:8000/api
